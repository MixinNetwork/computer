name: Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
env:
  POLYGONRPC: ${{ vars.POLYGONRPC }}
  SOLANARPC: ${{ vars.SOLANARPC }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7

    - name: Set up Go
      uses: actions/setup-go@v7
      with:
        go-version: 1.27

    - name: Build
      run: go build -o computer .

    - name: TestApps
      run: go test -v ./apps/...

    - name: TestComputer
      run: go test -timeout 20m -v ./solana/...
